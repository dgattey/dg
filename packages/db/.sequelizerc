const { resolve } = require('path');

module.exports = {
  config: resolve('database.config.js'),
  migrationsPath: resolve('migrations'),
  modelsPath: resolve('models'),
  seedersPath: resolve('seeders'),
};
