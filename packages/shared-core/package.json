{
  "devDependencies": {
    "@types/node": "catalog:",
    "dotenv-mono": "catalog:",
    "tsconfig": "workspace:*",
    "typescript": "catalog:"
  },
  "name": "shared-core",
  "private": true,
  "scripts": {
    "check": "biome check --write .",
    "check:types": "tsc",
    "clean": "rm -rf .turbo *.tsbuildinfo"
  }
}
