{
  "name": "tsconfig",
  "private": true,
  "files": ["base.json", "nextjs.json", "react.json"],
  "scripts": {
    "clean": "rm -rf .turbo *.tsbuildinfo",
    "format": "biome check --write ."
  }
}
