{
  "name": "api",
  "private": true,
  "scripts": {
    "format": "prettier --write .",
    "lint": "eslint . --cache --fix",
    "lint:types": "tsc"
  },
  "dependencies": {
    "shared-core": "workspace:*"
  },
  "devDependencies": {
    "dotenv-mono": "1.3.13",
    "eslint-config-dg": "workspace:*",
    "tsconfig": "workspace:*",
    "typescript": "5.3.3"
  }
}
