{
  "name": "webhooks",
  "private": true,
  "scripts": {
    "dev": "cloudflared tunnel run dg",
    "webhook": "NODE_ENV=development tsx src/webhookCommand.ts",
    "format": "prettier --write .",
    "lint": "eslint . --cache --fix",
    "lint:types": "tsc"
  },
  "devDependencies": {
    "cmd-ts": "0.13.0",
    "dotenv-mono": "github:dgattey/dotenv-mono#dgattey--fix-priority",
    "eslint-config-dg": "workspace:*",
    "node-fetch": "3.3.2",
    "shared-core": "workspace:*",
    "tsconfig": "workspace:*",
    "tsx": "3.12.10",
    "typescript": "5.2.2"
  }
}
