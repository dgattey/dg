version: '3'

services:
  web:
    container_name: web
    build:
      context: .
      dockerfile: dockerfile.dev
    ports:
      - '8000:8000'
      # These are for VSCode debug ports
      - '9929:9929'
      - '9230:9230'
    user: 'node'
    environment:
      - NODE_ENV=development
      - GATSBY_WEBPACK_PUBLICPATH=/
    volumes:
      - ./src:/home/node/app/src
      - /home/node/app/node_modules
